{{#if col4}}
<div class="col-md-4" id="{{_id}}">
    {{/if}}
    <div class="media-area main-raised m-0">
        <div class="media">
            <a class="float-left" href="#pablo">
                <div class="avatar">
                    <img class="media-object" src="../assets/img/logo.png" alt="...">
                </div>
            </a>
            <div class="media-body">
                <h5 class="media-heading">{{name}}
                    <br>
                    <div class="stats ml-auto">
                        <i class="material-icons delete-student-btn" style="top: 0%; cursor: pointer;" data-id="{{_id}}"
                            data-tuition="{{tuitionId}}">delete</i>
                        <i class="material-icons" style="top: 0%; cursor: pointer;">edit</i>
                    </div>
                </h5>
            </div>
        </div>
    </div>
    {{#if col4}}
</div>
{{/if}}